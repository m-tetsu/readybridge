# www.ready-bridge.com を正規ホストとする。
# ドメインをまたぐ301リダイレクト（http/非www/旧workers.devドメイン → https://www.ready-bridge.com）は
# Cloudflare Workers Static Assets の _redirects では絶対URLソースが使えないため設定不可（デプロイエラーになる）。
# Cloudflareダッシュボードの Rules → Redirect Rules（ゾーンレベル）で別途設定すること。
